query:
  description: "Query the [[ project_name ]] service."
  fields:
    query:
      required: true
      description: "The query string to send."
      example: "search term"
      selector:
        text:
